const arr = ['Якуб', 'Адам', 'Умар', 'Адлан', 'Руслан', 'Халид', 'Султан', 'Турпал', 'Аслан', 'Сахруддин']
let accumulator = [];
for (let i = 0; i < arr.length; i++) {
  if (arr[i][0] === 'А') [
    accumulator.push(arr[i])
  ]
}
console.log(accumulator)