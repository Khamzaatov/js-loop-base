const arr = ['Адам', 'Султан', 'Хамзат', 'Ризван', 'Руслан', 'Адлан', 'Юсуп', 'Умар', 'Альви', 'Хьусен']
for (let i = 0; i < arr.length; i++) {
  console.log(arr[i])
}
let i = 0;
while (i < arr.length) {
  console.log(arr[i]);
  i++
}